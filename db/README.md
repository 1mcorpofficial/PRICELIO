# Database

This folder will hold migrations, seeds, and schema references.

TODO:
- Draft initial schema for core tables (users, stores, products, offers, receipts).
- Add seed data for early dev.
- Document indexes and constraints.

## Current files
- `db/migrations/0001_init.sql` initial MVP schema.
- `db/schema.sql` snapshot of the latest schema.
